export class SpiceflowFetchError extends Error {
    status;
    passedValue;
    value;
    response;
    constructor(status, passedValue, response) {
        let message = String(passedValue?.message || '');
        if (!message) {
            if (typeof passedValue === 'object') {
                message = JSON.stringify(passedValue);
            }
            else {
                message = String(passedValue || '');
            }
        }
        super(message);
        this.status = status;
        this.passedValue = passedValue;
        this.value = passedValue;
        this.response = response;
    }
}
//# sourceMappingURL=errors.js.map